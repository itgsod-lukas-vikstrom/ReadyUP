// Generated by CoffeeScript 1.8.0
(function() {
  var CountDownTimer, post;

  $(function() {
    return CountDownTimer("19:25", "countdown");
  });

  CountDownTimer = function(time, id) {
    var end, selector, showRemaining, timer, _day, _hour, _minute, _second;
    selector = document.getElementById(id);
    end = new Date("01/29/2015 " + time);
    _second = 1000;
    _minute = _second * 60;
    _hour = _minute * 60;
    _day = _hour * 24;
    showRemaining = function() {
      var distance, hours, minutes, now;
      now = new Date();
      distance = end - now;
      if (distance <= 0) {
        clearInterval(timer);
        return;
      }
      hours = Math.floor((distance % _day) / _hour);
      minutes = Math.floor((distance % _hour) / _minute);
      if (minutes < 10) {
        minutes = "0" + minutes;
      }
      return selector.innerHTML = "" + hours + ":" + minutes;
    };
    return timer = setInterval(showRemaining, 1000);
  };

  post = function(path, params, method) {
    var form, hiddenField, key;
    method = method || "post";
    form = document.createElement("form");
    form.setAttribute("method", method);
    form.setAttribute("action", path);
    for (key in params) {
      if (params.hasOwnProperty(key)) {
        hiddenField = document.createElement("input");
        hiddenField.setAttribute("type", "hidden");
        hiddenField.setAttribute("name", key);
        hiddenField.setAttribute("value", params[key]);
        form.appendChild(hiddenField);
      }
    }
    document.body.appendChild(form);
    form.submit();
  };

}).call(this);
