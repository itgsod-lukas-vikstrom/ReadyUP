// Generated by CoffeeScript 1.8.0
(function() {
  var CountDownTimer, Popup;

  $(function() {
    var hund;
    hund = document.getElementById("time");
    CountDownTimer(hund.innerHTML, "countdown");
    return setInterval;
  });

  CountDownTimer = function(time, id) {
    var end, selector, showRemaining, timer, _day, _hour, _minute, _second;
    selector = document.getElementById(id);
    end = new Date("02/02/2015 " + time);
    _second = 1000;
    _minute = _second * 60;
    _hour = _minute * 60;
    _day = _hour * 24;
    showRemaining = function() {
      var distance, hours, minutes, now, seconds;
      now = new Date();
      distance = end - now;
      if (distance <= 0) {
        clearInterval(timer);
        alert('Alla är redo');
        return;
      }
      hours = Math.floor((distance % _day) / _hour);
      minutes = Math.floor((distance % _hour) / _minute);
      seconds = Math.floor((distance % _minute) / _second);
      if (minutes < 10) {
        minutes = "0" + minutes;
      }
      return selector.innerHTML = "" + hours + ":" + minutes + ":" + seconds;
    };
    return timer = setInterval(showRemaining, 1000);
  };

  Popup = function(name, time) {
    alert('Name = ' + name + ' URL = ' + url);
  };

}).call(this);
